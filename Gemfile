source "https://rubygems.org"

# Local preview only. GitHub Pages builds this repo with its own pinned Jekyll
# (3.x) and ignores this file, so the site sticks to features both versions
# support: data files, layouts, `relative_url`, and jekyll-seo-tag.
gem "jekyll", "~> 4.4"
gem "webrick", "~> 1.8"

group :jekyll_plugins do
  gem "jekyll-seo-tag", "~> 2.8"
end
